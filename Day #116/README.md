# Day 116

## Difficulty

Medium

## Problem Statement

Generate a finite, but an arbitrarily large binary tree quickly in O(1).

That is, `generate()` should return a tree whose size is unbounded but finite.

## Explanation

Design a generate() that produces a finite but arbitrarily large binary tree in O(1) time.

## Company

Jane Street
