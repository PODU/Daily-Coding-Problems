# Day 147

## Difficulty

Hard

## Problem Statement

Given a list, sort it using this method: `reverse(lst, i, j)`, which reverses `lst` from `i` to `j`.

## Explanation

Sort a list using only a subarray-reverse operation (reverse from index i to j) as the primitive.

## Company

Unattributed
