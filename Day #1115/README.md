# Day 1115

## Difficulty

Medium

## Problem Statement

Given an integer `n` and a list of integers `l`, write a function that randomly generates a number from `0` to `n-1` that isn't in `l` (uniform).

## Explanation

Uniformly sample a random integer in the range 0 to n-1 that is not present in a given exclusion list.

## Company

Google
