# Day 1004

## Difficulty

Hard

## Problem Statement

Given a list of numbers `L`, implement a method `sum(i, j)` which returns the sum from the sublist `L[i:j]` (including `i`, excluding `j`).

You can assume that you can do some pre-processing. `sum()` should be optimized over the pre-processing step.

## Example

### Input
```
L = [1, 2, 3, 4, 5], sum(1, 3)
```
### Output
```
sum([2, 3]), which is 5
```

## Explanation

Implement an optimized range-sum query method sum(i, j) over a list, allowing pre-processing so that each query is fast.

## Company

Goldman Sachs
