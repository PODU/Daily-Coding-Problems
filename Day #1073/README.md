# Day 1073

## Difficulty

Medium

## Problem Statement

Implement a job scheduler which takes in a function `f` and an integer `n`, and calls `f` after `n` milliseconds.

## Explanation

Build a job scheduler that invokes a given function after a specified delay in milliseconds.

## Company

Apple
