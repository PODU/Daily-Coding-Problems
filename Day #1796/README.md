# Day 1796

## Difficulty

Medium

## Problem Statement

Given an integer `n` and a list of integers `l`, write a function that randomly generates a number from `0` to `n-1` that isn't in `l` (uniform).

## Explanation

Generate a uniformly random integer in the range 0 to n-1 while excluding any values that appear in the list l.

## Company

Google
