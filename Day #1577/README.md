# Day 1577

## Difficulty

Hard

## Problem Statement

We say a number is sparse if there are no adjacent ones in its binary representation. For example, 21 (10101) is sparse, but 22 (10110) is not. For a given input N, find the smallest sparse number greater than or equal to N.

Do this in faster than O(N log N) time.

## Example

### Input
```
21
```
### Output
```
21
```

## Explanation

A number is sparse if its binary representation has no two adjacent ones; given N, find the smallest sparse number greater than or equal to N in faster than O(N log N) time.

## Company

Oracle
