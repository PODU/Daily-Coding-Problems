# Day 268

## Difficulty

Medium

## Problem Statement

Given a `32`-bit positive integer `N`, determine whether it is a power of four in faster than `O(log N)` time.

## Explanation

Determine whether a given 32-bit positive integer is a power of four, doing so in better than O(log N) time.

## Company

Indeed
