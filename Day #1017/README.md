# Day 1017

## Difficulty

Hard

## Problem Statement

Given a list, sort it using this method: `reverse(lst, i, j)`, which reverses `lst` from `i` to `j`.

## Explanation

Sort a list using only a reverse(lst, i, j) operation that reverses the sublist between indices i and j.

## Company

Unattributed
